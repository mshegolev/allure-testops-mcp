# Milestone v0.4 — Audit & Completion

**Verdict:** ✅ COMPLETE — 4/4 phases delivered, shipped, and independently verified.
**Audited:** 2026-06-05 (independent re-verification, not trusting prior claims).
**Theme:** Quality & robustness hardening of the test-case write tools.

## Phase outcomes

| # | Phase | Version | Evidence verified |
|---|-------|---------|-------------------|
| 3 | Version single-source-of-truth | 0.4.0 | `__version__` derives from `importlib.metadata`; consistency test guards `pyproject` == `server.json` ×2 |
| 4 | Update verb hardening (PATCH→PUT) | 0.4.0 | `client.put` + `_patch_or_put`; 405-only fallback; unit-tested |
| 2 | Gated live integration tests | 0.4.0 | `tests/integration/test_write_live.py` present; deselected/self-skipping |
| 1 | Status/layer name→id resolution | 0.4.2 | `_list_refs`/`_resolve_ref`/`_project_id_of`; endpoints confirmed live; resolver verified live read-only |

## Independent audit results
- **Tests:** 129 pass, 2 integration deselected (`pytest -q`).
- **Code:** all four deliverables present in source (grep-verified).
- **Artifacts:** 4 phase SUMMARY files under `.planning/phases/`.
- **Distribution:** PyPI carries `0.4.0`, `0.4.1`, `0.4.2`; MCP Registry `0.4.2` (`isLatest`).
- **CI:** green on Python 3.10 / 3.11 / 3.12 for every release PR (#4–#9).

## Known residual (carried forward, not a milestone failure)
- **Live write mutation unverified end-to-end.** The aiqa-core token is read-scoped (create → HTTP 403, surfaced cleanly). Resolver + read path are live-verified; write body shapes are DTO-grounded + unit-tested. Closing this needs a write-scoped token + sandbox project, then `pytest -m integration`.

## Process notes
- `.planning/` was bootstrapped retroactively; the milestone ran phases 3/4/2 first (mock-verifiable), deferred Phase 1 until live Allure access existed, then closed it. Evidence-gated throughout — nothing unverifiable was shipped into a published package.

## Candidate next milestone (v0.5) — for user scoping, NOT auto-started
- Live write-path verification (write-scoped token) — exercise create/update/delete + name resolution against a sandbox project.
- Tag write tools (`/testcase/{id}/tag`) and custom fields (`/cfv`) — separate endpoints discovered during Phase 1 research.
- Restore (`deleted=false`) / bulk operations.
